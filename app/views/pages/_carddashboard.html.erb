  <% yachts.each do |yacht| %>
<div class="card-dashboard">
    <div class="card-category" >
      <div class="card-image" style="background-image: linear-gradient(rgba(0,0,0,0), rgba(0,0,0,0)), url('<%= cl_image_path yacht.photo1 %>')">
        <ul class="list-inline">
          <li class="list-inline-item"></li>
        </ul>
      </div>
      <div class="card-description">
          <ul class="name-edit">
            <li> <h2><%= yacht.name %></h2></li>
          <li>Edit&nbsp; <%= link_to "<i class='fas fa-pen'></i>".html_safe, edit_yacht_path(yacht.id) %></li>
          </ul>
        <ul class="list-inline">
          <li class="list-inline-item"><i class="fas fa-anchor"></i>&nbsp;<%= yacht.port.address %></li>
          <li class="list-inline-item"><i class="fas fa-users"></i>&nbsp;<%= yacht.passengers_capacity %></li>
          <li class="list-inline-item"><i class="fas fa-euro-sign"></i>&nbsp;<%= number_with_delimiter(yacht.price, separator: " ") %></li>
        </ul>
      </div>
    </div>
      <% yacht.bookings.each do |booking| %>
    <div class="bookinguser">
      <% total =  (booking.end_date - booking.start_date)*booking.yacht.price %></p>
      <div class="booking-user">
        <p><%= cl_image_tag booking.user.photo, class: "avatarmyyatchs" %></p>
        <p><strong><%= booking.user.first_name %><br/><%= booking.user.last_name %></strong><br/></p>
        <p><strong>Start date:&nbsp;</strong><br/><%= booking.start_date.to_s(:rfc822) %></p><br/>
        <p><strong>End date:&nbsp;</strong><br/><%= booking.end_date.to_s(:rfc822) %></p>
        <p><strong>Total price:&nbsp;</strong><br/><%= number_with_delimiter((total*1.2).to_i, separator: " ") %></strong> <em><i class="fas fa-euro-sign"></i>&nbsp;&nbsp;incl. taxes</em>
      </div>
    </div>
      <% end %>
</div>
  <% end %>
